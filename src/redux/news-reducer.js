

let initialState = {
    news: [],
    pageSize: 10,
    totalUsersCount: 0,
    currentPage: 1,
    // totalUsersCount: 0,
    // currentPage: 1,
    // isFetching: false,
    // followingInProgress: [],
  };

  
  const newsReducer = (state = initialState, action) => {


  }

  export default newsReducer;